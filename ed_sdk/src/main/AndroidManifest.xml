<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"
        tools:ignore="ExactAlarm" />

    <application>
        <receiver android:name="com.edapp.habittracker.domain.ReminderReceiver" />
        <receiver
            android:name="com.edapp.habittracker.domain.BootReceiver"
            android:exported="true">

            <!-- If your receiver needs BOOT COMPLETED -->
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>

        </receiver>

        <!-- Habit home-screen App Widget (RemoteViews) -->
        <receiver
            android:name="com.edapp.habittracker.ui.widgets.HabitWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/habit_widget_info" />
        </receiver>

        <!-- Widget configuration activity -->
        <activity
            android:name="com.edapp.habittracker.ui.widgets.HabitWidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.HabitTracker.Widget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
    </application>

</manifest>
